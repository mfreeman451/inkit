apiVersion: gateway.networking.k8s.io/v1beta1
kind: ReferenceGrant
metadata:
  name: allow-carverauto-web-routes
  namespace: inkit
spec:
  from:
    - group: gateway.networking.k8s.io
      kind: HTTPRoute
      namespace: carverauto-web
  to:
    - group: ""
      kind: Service
      name: inkit
