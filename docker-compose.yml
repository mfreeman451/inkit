services:
  app:
    build: .
    ports:
      - "${INKIT_BIND:-0.0.0.0}:${INKIT_PORT:-4000}:4000"
    environment:
      PHX_SERVER: "true"
      PHX_HOST: "${PHX_HOST:-localhost}"
      PHX_SCHEME: "${PHX_SCHEME:-http}"
      PHX_URL_PORT: "${PHX_URL_PORT:-4000}"
      PORT: "4000"
      DATABASE_PATH: "/data/inkit.db"
      UPLOAD_DIR: "/data/uploads"
      SECRET_KEY_BASE: "${SECRET_KEY_BASE:-hECd1sv7C6D9chUdrq1PEo15vYNN6YN60hUqZHA8NWeLY3+/nkMn+ud64/7u7Ul1}"
    volumes:
      - inkit_data:/data

volumes:
  inkit_data:
